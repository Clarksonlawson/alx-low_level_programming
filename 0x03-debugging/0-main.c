#include "main.h"

/**
 * main - Tests the function that prints if an integer is positive or negative
 *
 * Return: 0
 */

int main(void)
{
	int i;

	i = 0;  /* Change the value to 0 to test the function with 0 */
	positive_or_negative(i);

	return (0);
}

